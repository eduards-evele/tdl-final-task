name: My pipeline
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - api-spec.yml
jobs:
  my_second_job:
    name: My first job
    runs-on: self-hosted
    steps:
    - run: echo Hello world!